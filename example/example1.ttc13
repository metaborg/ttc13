process Challenge =
  start;
  read;
  (specifyRules
  || implementExample
  || complain);
  runExample;
  end

instance
  start;
  read;
  (specifyRules
  || implementExample
  || complain);
  runExample;
  end;
  token